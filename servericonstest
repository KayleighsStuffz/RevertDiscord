:root {
  --guildbar-avatar-size: 64px;
  --guildbar-folder-size: 64px;

  --guildbar-icon-gap: 4px;
  --custom-guild-list-padding: 8px;

  --custom-guild-list-width: calc(
    var(--guildbar-avatar-size)
    + (var(--custom-guild-list-padding) * 2)
  );

  --custom-guild-item-height: calc(
    var(--guildbar-avatar-size)
    + var(--guildbar-icon-gap)
  );

  --custom-folder-preview-gap: 0px;
}

/* =========================================================
   MAIN SERVER BAR
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) {
  width: var(--custom-guild-list-width) !important;
  min-width: var(--custom-guild-list-width) !important;
}

/* =========================================================
   SERVER LIST ITEMS
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="listItem"] {
  box-sizing: border-box !important;

  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: var(--custom-guild-list-width) !important;
  height: var(--custom-guild-item-height) !important;

  margin: 0 !important;
  padding: 0 !important;
}

/* Direct server-item wrappers */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="listItem"] > [class*="wrapper"] {
  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  min-width: var(--guildbar-avatar-size) !important;
  min-height: var(--guildbar-avatar-size) !important;

  margin: 0 auto !important;
}

/* =========================================================
   SERVER ICON CONTAINERS
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) :is(
  [class*="listItemWrapper"],
  [class*="blobContainer"]
) {
  position: relative !important;

  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  min-width: var(--guildbar-avatar-size) !important;
  min-height: var(--guildbar-avatar-size) !important;

  margin: 0 auto !important;

  /*
   * The badge must be allowed outside the icon.
   * The foreignObject handles the circular clipping instead.
   */
  overflow: visible !important;
}

/* =========================================================
   CIRCULAR ICON MASKING
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="childWrapper"] {
  box-sizing: border-box !important;

  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  border-radius: 50% !important;
  overflow: hidden !important;
}

/*
 * Allow badges outside the SVG while clipping only the
 * foreignObject containing the server image.
 */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) svg:not([class*="placeholderMask"]) {
  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  overflow: visible !important;
}

/*
 * Disable Discord's squircle mask.
 * The border-radius now provides the circular clipping.
 */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) svg:not([class*="placeholderMask"]) > foreignObject {
  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  mask: none !important;
  clip-path: none !important;

  border-radius: 50% !important;
  overflow: hidden !important;
}

/* Ensure the contents completely fill the foreignObject */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) svg foreignObject > div {
  box-sizing: border-box !important;

  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: 100% !important;
  height: 100% !important;

  border-radius: 50% !important;
  overflow: hidden !important;
}

/* Server images */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) :is(
  svg foreignObject img,
  img[class*="icon"]
) {
  display: block !important;

  width: 100% !important;
  height: 100% !important;

  object-fit: cover !important;
  border-radius: 50% !important;
}

/*
 * Keep icons circular while hovered and selected.
 * Discord normally changes these into rounded squares.
 */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) :is(
  [class*="blobContainer"],
  [class*="childWrapper"],
  [class*="circleIconButton"]
) {
  border-radius: 50% !important;
}

/* =========================================================
   DISCORD HOME BUTTON
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="tutorialContainer"] {
  box-sizing: border-box !important;

  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: var(--custom-guild-list-width) !important;
  height: var(--custom-guild-item-height) !important;

  margin: 0 !important;
  padding: 0 !important;
}

/* Centre every relevant home-button wrapper */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="tutorialContainer"] :is(
  [class*="listItem"],
  [class*="wrapper"],
  [class*="listItemWrapper"]
) {
  box-sizing: border-box !important;

  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  margin: 0 auto !important;
  padding: 0 !important;

  left: auto !important;
  right: auto !important;

  translate: none !important;
  transform: none !important;
}

/* Discord circle */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="tutorialContainer"] [class*="childWrapper"] {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: var(--guildbar-avatar-size) !important;
  height: var(--guildbar-avatar-size) !important;

  margin: 0 !important;
  padding: 0 !important;

  border-radius: 50% !important;
}

/* Discord logo */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="tutorialContainer"] [class*="childWrapper"] > svg {
  position: static !important;

  display: block !important;

  width: 36px !important;
  height: 36px !important;

  margin: auto !important;

  inset: auto !important;
  translate: none !important;
  transform: none !important;
}

/* =========================================================
   NOTIFICATION AND PING BADGES
   ========================================================= */

/* Lower-right ping badges */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="lowerBadge"] {
  position: absolute !important;

  right: -4px !important;
  bottom: -4px !important;

  left: auto !important;
  top: auto !important;

  z-index: 20 !important;

  translate: none !important;
  transform: none !important;

  overflow: visible !important;
}

/* Upper-right event badges */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="upperBadge"] {
  position: absolute !important;

  right: -4px !important;
  top: -4px !important;

  left: auto !important;
  bottom: auto !important;

  z-index: 20 !important;

  translate: none !important;
  transform: none !important;

  overflow: visible !important;
}

/* Number badge styling */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="numberBadge"] {
  box-sizing: border-box !important;

  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  min-width: 22px !important;
  height: 22px !important;

  padding: 0 6px !important;

  border: 3px solid
    var(--background-base-lowest, var(--background-tertiary, #111214))
    !important;

  border-radius: 11px !important;

  font-size: 12px !important;
  line-height: 16px !important;

  overflow: visible !important;
}

/* =========================================================
   FOLDER BUTTONS
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="folderButton"] {
  width: var(--guildbar-folder-size) !important;
  height: var(--guildbar-folder-size) !important;

  overflow: visible !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="folderHeader"] {
  width: var(--guildbar-folder-size) !important;
  height: var(--guildbar-folder-size) !important;
}

/* Folder icon */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="folderIcon"] {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: var(--guildbar-folder-size) !important;
  height: var(--guildbar-folder-size) !important;

  border-radius: 50% !important;
  background-color: var(--bg-mod-subtle) !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="folderIcon"] svg {
  width: 30px !important;
  height: 30px !important;
}

/* =========================================================
   CLOSED-FOLDER PREVIEWS
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="folderPreviewWrapper"] {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;

  width: var(--guildbar-folder-size) !important;
  height: var(--guildbar-folder-size) !important;

  border-radius: 50% !important;
  overflow: hidden !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class$="folderPreview"] {
  width: calc(
    (var(--guildbar-folder-size) * 2 / 3)
    + var(--custom-folder-preview-gap)
  ) !important;

  height: calc(
    (var(--guildbar-folder-size) * 2 / 3)
    + var(--custom-folder-preview-gap)
  ) !important;
}

/* Mini icons inside a closed folder */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="iconSizeMini"] {
  width: calc(var(--guildbar-folder-size) / 3) !important;
  height: calc(var(--guildbar-folder-size) / 3) !important;

  overflow: hidden !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="iconSizeMini"] [class*="acronym"] {
  line-height: calc(var(--guildbar-folder-size) / 3) !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="iconSizeMini"][style*="font-size: 12px;"] {
  font-size: 10px !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="iconSizeMini"][style*="font-size: 10px;"] {
  font-size: 7px !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="iconSizeMini"][style*="font-size: 8px;"] {
  font-size: 6px !important;
}

/* Folder-preview images */
:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) :is(
  [class*="folderPreview"] img,
  [class*="folderPreviewGuildIcon"]
) {
  display: block !important;

  width: 100% !important;
  height: 100% !important;

  object-fit: cover !important;
}

/* =========================================================
   EXPANDED FOLDERS
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="isExpanded"] [class*="stack"] {
  height: auto !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="isExpanded"] [class*="folderGroupBackground"] {
  border-bottom-left-radius: 24px !important;
  border-bottom-right-radius: 24px !important;
}

/* =========================================================
   DRAG-AND-DROP INDICATORS
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="dragInner"] {
  width: calc(var(--guildbar-avatar-size) - 8px) !important;
  height: calc(var(--guildbar-avatar-size) - 8px) !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="dragOver"][class*="target"]::before {
  left: calc(var(--guildbar-avatar-size) / 4) !important;
  width: 100% !important;
}

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) [class*="dragOver"][class*="centerTarget"]::before {
  top: calc(var(--guildbar-avatar-size) / 2) !important;
}

/* =========================================================
   UNREAD DIRECT MESSAGES
   ========================================================= */

:is(
  nav[class*="guilds"],
  nav[aria-label="Servers"],
  nav[data-list-id="guildsnav"]
) #guild-list-unread-dms > div {
  height: var(--custom-guild-item-height) !important;
}
